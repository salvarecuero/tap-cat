We need to work on a few stuff:

Make sure that cat doesn't overlap with other elements (always without generating page scroll). Cat should always be fully visible, it just should resize itself to fit the rest of the screen. Buyable items should use only one row if there's not enough height, and in that case they should be horizontal scrollable (on a clean way, for a game/app, not just a div that I need to scroll manually).
